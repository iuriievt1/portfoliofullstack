import { AppShell } from "@/components/app-shell";
import { Card, CardContent } from "@otpbank/ui";

export default function Page() {
  return (
    <AppShell title="Support users">
      <Card>
        <CardContent className="py-10 text-sm text-slate-500">Support search and visible-assist review tooling lands here.</CardContent>
      </Card>
    </AppShell>
  );
}
